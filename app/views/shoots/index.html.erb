<% provide(:title, 'Work') %>

<% content_for :filters do %>
	<ul class="filters">
    <%= render 'shoots/filters' %>
	</ul>
<% end %>

<ul class="grid thumbs">
  <%= render 'shoots/shoot_li' %>
</ul>

<% if current_user %>
<div class="content">
  <%= link_to 'Upload Shoot', new_shoot_path, 
  :class => "btn btn-large btn-primary" %>
</div>
<% end %>
